spack:
  view: false
  concretization: separately
  modules:
    tcl:
      whitelist:
        - brayns
        - brion+python
        - meshball
        - ultraliser
  specs:
    - brayns%gcc ^ospray%intel ^intel-tbb%intel
    - brion +python
    - ultraliser ^hdf5~mpi
