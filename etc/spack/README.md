# Spack main config scope

This Spack's top-level configuration scope. Everything except Spack's bare-bones
default configuration is included from here.

The main configuration scope defines other scopes.
