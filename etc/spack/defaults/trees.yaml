# A tree is a collection of packages which should be projected
trees:
  tree-foo: 
    single:
      - mpi     #each of these is a query spec: get all matching specs
      - bzip2
    transitive:
      - boost   #likewise each of these is a query spec
