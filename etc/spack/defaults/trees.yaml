# -------------------------------------------------------------------------
# This file controls default symlink projections
#
# Settings here are versioned with Spack and are intended to provide
# sensible defaults out of the box. Spack maintainers should edit this
# file to keep it current.
#
# Users can override these settings by editing the following files.
#
# Per-spack-instance settings (overrides defaults):
#   $SPACK_ROOT/etc/spack/packages.yaml
#
# Per-user settings (overrides default and site settings):
#   ~/.spack/packages.yaml
# -------------------------------------------------------------------------
trees:
  all:
    descriptor: "${PACKAGE}-${VERSION}"
    multiply:
      - prepend:mpi #since there is no entry, we use the descriptor for 'all' (careful about omitting 'mpi', and in fact you can omit any multiplier that's already mentioned, which would be all of the multipliers in this case since you are referring back to the same descriptor)
      - append:bzip2 #if we didnt want to use exactly the same package descriptor as "bzip2" we could create and refer to e.g. "bzip2/bzip2_multiplier"
      - basename:zlib
#   projection:
#     tree-foo: 
#       single:
#         - mpi     #each of these is a query spec: get all matching specs
#         - bzip2
#       transitive:
#         - pkg A   #likewise each of these is a query spec
#         - pkg B
  bzip2:
    descriptor: "ARRRRRR-${PACKAGE}"
