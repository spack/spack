bootstrap-resource:
  # If set to false Spack will not fetch bootstrap resources but instead
  # proceed without fetching said resources
  enable: true
  # Trusted sources to acquire bootstrap resources from
  sources:
  - name: 'github-actions-v0.1'
    resource_layout: $spack/share/spack/resource/github-actions-v0.1
