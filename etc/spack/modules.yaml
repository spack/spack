modules:
  enable:
    - tcl
  tcl:
    intelmpi::
      environment:
        prepend_path:
          PATH: '$env(I_MPI_ROOT)/intel64/bin'
          LD_LIBRARY_PATH: '$env(I_MPI_ROOT)/intel64/lib'
          MANPATH: '$env(I_MPI_ROOT)/intel64/man'
          CMAKE_PREFIX_PATH: '$env(I_MPI_ROOT)/intel64'


