# Spack site configuration scope

This is the "site" configuration scope for Spack. By default, it sits
at *lower* priority than the spack scope in `etc/spack` within the Spack
directory, and *lower* priority than user configuration.
