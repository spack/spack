# DEPRECATED: remove this in v0.20
# See `exclude.yaml` for the new syntax
enable:
  - lmod
lmod:
  core_compilers:
    - 'clang@3.3'
  hierarchy:
    - mpi
  blacklist:
    - callpath

  all:
    autoload: direct
