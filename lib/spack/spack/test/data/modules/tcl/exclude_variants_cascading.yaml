enable:
  - tcl
tcl:
  variants: all
  all:
    autoload: direct
    filter:
      exclude_variants:
        - fortran
        - debug

  mpileaks:
    filter:
      exclude_variants:
        - unk
        - debug
        - shared
