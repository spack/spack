spack:
  view: false
  packages:
    julia:
      version:
      - 1.9.3
  definitions:
  - arch:
    - "%gcc"

  - packages:
    - julia

  - targets:
    - "target=x86_64_v3"

  specs:
  - matrix:
    - - $packages
    - - $arch
    - - $targets
  ci:
    pipeline-gen:
    - build-job:
        tags: ["gcp"]
        image: "ghcr.io/spack/e4s-centos-7:v2023-10-30"

  cdash:
    build-group: "GCP Packages"
