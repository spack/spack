spack:
  view: false
  definitions:
  - arch:
    - %gcc

  - packages:
    - stream

  - targets:
    - target=x86_64_v3

  specs:
  - matrix:
    - - $packages
    - - $arch
    - - $targets
  ci:
    pipeline-gen:
    - build-jobs:
        tags: [gcp]
  cdash:
    build-group: GCP Packages
