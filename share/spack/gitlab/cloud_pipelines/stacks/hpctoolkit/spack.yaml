spack:
  view: false
  
  concretizer:
    reuse: false
    unify: false
    targets:
      granularity: generic
  
  config:
    concretizer: clingo
    install_tree:
      root: /home/software/spack
      padded_length: 512
      projections:
        all: '{architecture}/{compiler.name}-{compiler.version}/{name}-{version}-{hash}'

  packages:
    all:
      target: [x86_64_v3]

  specs:
    - matrix:
      - - libmonitor +hpctoolkit ~dlopen ~commrank
      - ['@2021.11.08', '@:']
    - matrix:
      - - hpctoolkit +mpi +viewer
      - - +papi
        - +papi ^papi @6.0.0.1
        - ~papi
        - ~papi ^libpfm4 @4.11.0
      - # Latest release with latest compatible dependencies
        - '@:'
        # Latest release with minimum supported dependencies
        - ^autoconf @2.69
          ^automake @1.15.1
          ^libtool @2.4.6
          ^boost @1.77.0
          ^libiberty @2.37
          ^elfutils @0.186
          ^dyninst @12.1.0
          ^intel-tbb @2020.3
          ^bzip2 @1.0.8
          ^xz @5.2.5
          ^zlib @1.2.12
          ^libunwind @1.6.2
          ^xerces-c @3.2.3
          ^memkind @1.12.0
          ^intel-xed @12.0.1

  mirrors: { "mirror": "s3://spack-binaries/develop/hpctoolkit" }

  gitlab-ci:
    script:
      - . "./share/spack/setup-env.sh"
      - spack --version
      - cd ${SPACK_CONCRETE_ENV_DIR}
      - spack env activate --without-view .
      - spack config add "config:install_tree:projections:${SPACK_JOB_SPEC_PKG_NAME}:'morepadding/{architecture}/{compiler.name}-{compiler.version}/{name}-{version}-{hash}'"
      - if [[ -r /mnt/key/intermediate_ci_signing_key.gpg ]]; then spack gpg trust /mnt/key/intermediate_ci_signing_key.gpg; fi
      - if [[ -r /mnt/key/spack_public_key.gpg ]]; then spack gpg trust /mnt/key/spack_public_key.gpg; fi
      - spack -d ci rebuild
    
    variables:
      CI_JOB_SIZE: default

    mappings:
      - match: ['os=centos7']
        runner-attributes:
          image: { "name": "ghcr.io/spack/centos7:latest", "entrypoint": [""] }
          tags: ["spack", "x86_64_v3"]
      - match: ['os=fedora36']
        runner-attributes:
          image: { "name": "ghcr.io/blue42u/fedora36:latest", "entrypoint": [""] }
          tags: ["spack", "x86_64_v3"]
      - match: ['os=opensuse_leap15']
        runner-attributes:
          image: { "name": "ghcr.io/spack/leap15:latest", "entrypoint": [""] }
          tags: ["spack", "x86_64_v3"]
      - match: ['os=rhel8']
        runner-attributes:
          image: { "name": "ghcr.io/blue42u/ubi8:latest", "entrypoint": [""] }
          tags: ["spack", "x86_64_v3"]
      - match: ['os=ubuntu20.04']
        runner-attributes:
          image: { "name": "ghcr.io/spack/ubuntu-focal:latest", "entrypoint": [""] }
          tags: ["spack", "x86_64_v3"]

    broken-specs-url: "s3://spack-binaries/broken-specs"

    service-job-attributes:
      before_script:
        - . "./share/spack/setup-env.sh"
        - spack --version
      image: { "name": "ghcr.io/spack/ubuntu-jammy:latest", "entrypoint": [""] }
      tags: ["spack", "public", "x86_64_v3"]

    signing-job-attributes:
      image: { "name": "ghcr.io/spack/notary:latest", "entrypoint": [""] }
      tags: ["spack", "aws"]
      script:
        - aws s3 sync --exclude "*" --include "*spec.json*" ${SPACK_REMOTE_MIRROR_OVERRIDE}/build_cache /tmp
        - /sign.sh
        - aws s3 sync --exclude "*" --include "*spec.json.sig*" /tmp ${SPACK_REMOTE_MIRROR_OVERRIDE}/build_cache