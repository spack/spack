<?xml version="1.0" encoding="UTF-8"?>
<Site BuildName="{{ buildname }}"
      BuildStamp="{{ buildstamp }}"
      Name="{{ site }}"
      Generator="{{ generator }}"
      Hostname="{{ hostname }}"
      OSName="{{ osname }}"
      OSRelease="{{ osrelease }}"
      VendorString="{{ target }}"
>
