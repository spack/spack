--- a/src/GeneRax/GeneRaxCore.cpp
+++ b/src/GeneRax/GeneRaxCore.cpp
@@ -480,8 +480,11 @@ void GeneRaxCore::speciesTreeSupportEstimation(GeneRaxInstance &instance)
         eqpicOutput, 
         supportOutput,
         supportTripletOutput);
+    Logger::timed << "Entering barrier before copy" << std::endl;
     ParallelContext::barrier();
+    Logger::timed << "Copying File " << eqpicOutput << " to " << instance.speciesTree << std::endl;
     FileSystem::copy(eqpicOutput, instance.speciesTree, true);
+    Logger::timed << "Copy finished" << std::endl;
      
   }
   ParallelContext::barrier();
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -11,9 +11,7 @@ if(JOINTSEARCH_BUILD_AS_LIBRARY)
   set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -DJOINTSEARCH_BUILD_AS_LIB=ON")
 endif()
 
-set(CMAKE_CXX_FLAGS  "${CMAKE_CXX_FLAGS} -Wall")
-set(CMAKE_CXX_FLAGS  "${CMAKE_CXX_FLAGS} -fno-omit-frame-pointer")
-set(CMAKE_CXX_FLAGS  "${CMAKE_CXX_FLAGS} -O3")
+set(CMAKE_CXX_FLAGS  "${CMAKE_CXX_FLAGS} -Wall -O0 -g")
 
 include(CheckCXXCompilerFlag)
 CHECK_CXX_COMPILER_FLAG(-std=c++14 CXX_STANDARD_14_SUPPORT -g)
