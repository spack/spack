diff -ur a/install_components/Linux_ppc64le/21.5/compilers/bin/makelocalrc b/install_components/Linux_ppc64le/21.5/compilers/bin/makelocalrc
--- a/install_components/Linux_ppc64le/21.5/compilers/bin/makelocalrc	2021-05-29 16:31:07.000000000 -0600
+++ b/install_components/Linux_ppc64le/21.5/compilers/bin/makelocalrc	2021-07-01 08:37:34.136595453 -0600
@@ -31,8 +31,8 @@
 ##      [-ofeddir dir]
 ##
 
-# Just in case, add /usr/bin and /bin to PATH
-PATH=/usr/bin:/bin:$PATH
+# Just in case, append /usr/bin and /bin to PATH
+PATH=${PATH:+$PATH:}/usr/bin:/bin
 LANG=en_US.UTF-8
 
 # set TMPDIR if environment variable $TMPDIR is not set
diff -ur a/install_components/install b/install_components/install
--- a/install_components/install	2021-05-29 16:46:52.000000000 -0600
+++ b/install_components/install	2021-07-01 08:49:17.804854261 -0600
@@ -369,11 +369,11 @@
 
 if test $network_install -eq 0; then
     if test -x $INSTALL_DIR/$arch/$release/compilers/bin/makelocalrc ; then
-        $INSTALL_DIR/$arch/$release/compilers/bin/makelocalrc -x $INSTALL_DIR/$arch/$release/compilers/bin $docuda $dostdpar
+        $INSTALL_DIR/$arch/$release/compilers/bin/makelocalrc -x $INSTALL_DIR/$arch/$release/compilers/bin $docuda $dostdpar -gcc $SPACK_CC -gpp $SPACK_CXX -g77 $SPACK_F77
     fi
 else
     if test -x $INSTALL_DIR/$arch/$release/compilers/bin/makelocalrc ; then
-        $INSTALL_DIR/$arch/$release/compilers/bin/makelocalrc  -x $INSTALL_DIR/$arch/$release/compilers/bin -net $DEFAULT_SHARED $docuda $dostdpar
+        $INSTALL_DIR/$arch/$release/compilers/bin/makelocalrc  -x $INSTALL_DIR/$arch/$release/compilers/bin -net $DEFAULT_SHARED $docuda $dostdpar -gcc $SPACK_CC -gpp $SPACK_CXX -g77 $SPACK_F77
     fi
 fi
 rm -f $INSTALL_DIR/$arch/$release/compilers/glibc_version
